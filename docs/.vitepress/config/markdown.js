// markdown 配置
const markdownConfig = {

    // 代码行号
    lineNumbers: true,
    // 右侧目录展示层级
    toc: { level: [1, 2, 3] },
    // 默认高亮语言
    defaultHighlightLang: 'java'
}

export default markdownConfig